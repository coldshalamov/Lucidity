# lucidity-relay-agent

Desktop-side bridge that connects a local `lucidity-host` TCP listener to a `lucidity-relay` instance over WebSockets.

This is intended to be started on the desktop alongside WezTerm/Lucidity.

