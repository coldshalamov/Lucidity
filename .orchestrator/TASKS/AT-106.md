# AT-106 — Typed Pane Exit Signal

OWNER: Claude Code / exact Claude Fable 5 / xhigh, isolated mutation worktree; different-family review required
WORKTREE: `C:\Users\93rob\Documents\GitHub\Lucidity-worktrees\AT-106-exit-signal`
BRANCH: `codex/AT-106-exit-signal`
BASE CRITERIA: exact integration commit containing accepted AT-099, AT-105, AT-107, and AT-103 commits. The controller records and asserts the exact launch SHA in the external ledger and worktree receipt.

## Objective

Preserve the already-observed child exit result long enough to publish one typed, pane-correlated exit notification before pane removal, without changing stock exit behavior.

## Exclusive write lease

- `mux/src/localpane.rs`
- `mux/src/lib.rs`
- the minimal exhaustive `MuxNotification` match sites required to compile, with behavior-neutral handling outside Agent mode
- focused tests under those packages

No PTY spawning, GUI geometry, close policy, Cargo manifest, product-host, or protocol edits.

This task starts only after AT-105 has released `frontend.rs` and the close-policy portion of `termwindow/mod.rs`. Any behavior-neutral exhaustive notification match edit there is made against the integrated AT-105 base; the close policy itself is not reopened.

## Requirements

1. Publish exactly one typed exit observation containing `PaneId`, exit code/success, and whether Lucidity/WezTerm requested the kill, before `PaneRemoved` can be observed.
2. Retain enough internal state that repeated dead/prune checks cannot duplicate or lose the observation.
3. Preserve `ExitBehavior::{Close,CloseOnCleanExit,Hold}` display and pruning semantics in stock mode.
4. Define ordering tests for natural zero, natural non-zero, explicit kill, waiter error, repeated prune, and pane removal.
5. Give the Agent host a public notification it can subscribe to; do not make GUI code authoritative for runtime state.

## Acceptance

Focused mux tests plus `cargo test -p mux`, `cargo check -p wezterm-gui`, and stock native smoke. Controller must reproduce notification-before-removal ordering.

## Stop and escalate

Any protocol/schema change, Job Object work, new dependency, or stock-visible lifecycle regression.
