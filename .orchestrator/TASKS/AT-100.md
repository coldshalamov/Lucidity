# AT-100 — Cross-Family Baseline Review

OWNER: Claude Code / exact Claude Fable 5 / xhigh, fresh read-only review session
TARGET: exact AT-099 candidate commit after its worker has stopped and returned
BASE CRITERIA: the controller records the candidate SHA, parent SHA, worktree path, and clean review start in the external ledger and launch receipt.

## Objective

Adversarially review the complete AT-099 baseline before any dependent worktree is dispatched. Prove that the package/resource skeleton is compile-safe, minimal, stock-preserving, license-compatible, and a real handoff for AT-101/102/103/105/107/108 rather than a collection of placeholders that later workers cannot legally replace.

## Authority and mutation policy

- Read `.orchestrator/ADRS/**`, `INTERFACES.md`, `RISKS.md`, `VERIFICATION.md`, `DESIGN/NATIVE_UI_CONTRACT.md`, AT-099, and the exact candidate diff/commit.
- Read product sources, Cargo metadata, PE resources, test output, and dependency/license data needed to verify the candidate.
- Run read-only builds/checks/tests and native inspection commands that do not mutate repository sources.
- Do not edit, format, stage, commit, amend, merge, create a branch, or update the ledger. Generated build output is allowed; source mutation is not.

## Required attacks

1. Confirm the root topology adds exactly the three accepted packages and no speculative wrapper tree or extra crate.
2. Confirm every top-level module and the thin `agent_terminal::chrome::run() -> anyhow::Result<()>` binary handoff compiles and is legally replaceable by its named downstream lease without `lib.rs`/binary-wrapper repair.
3. Confirm `agent-terminal` does not depend on binary-only `wezterm-gui`, Cargo emits no ignored-dependency warning, and AT-101 retains the later serialized dependency lease.
4. Review exact root Cargo, `pty/Cargo.toml`, and `Cargo.lock` deltas; `jobapi2` is the only upstream dependency-feature change and no version changed.
5. Verify IDs, state orthogonality, `PendingIdentity`, exit observation, framing/queue constants, reserved SetUserVar name, and internal `CloseIntent` package ownership against frozen contracts.
6. Inspect the Agent PE subsystem, manifest/DPI/UTF-8, `0x101` icon, version data, AUMID/class/title, update-disable policy, and runtime-file freshness behavior. Confirm the icon implements the frozen Weld Frame and is not the stock WezTerm icon.
7. Check MIT compatibility and the exact new dependency license record.
8. Re-run the packet's Rust and stock-parity commands. Report raw failures/warnings and reject warning-dependent baselines.
9. Inspect the candidate write set for product implementation smuggled into skeleton modules or protected `.orchestrator/**`/`HACKATHON/**` changes.

## Return

Findings first with severity and exact file/line evidence; `PROCEED`, `PROCEED_AFTER_FIXES`, or `BLOCK`; candidate/parent SHA; exact commands and raw outcomes; diff/worktree receipt; dependency/license/resource findings; handoff failures; recommended controller reproductions. Worker narration cannot accept the commit.

## Gate

The controller must reproduce every blocker, record an accepted review, and integrate/accept AT-099 before dispatching AT-105, AT-107, AT-103, or AT-104.
