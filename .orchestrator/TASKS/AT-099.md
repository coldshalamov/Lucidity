# AT-099 — Frozen Baseline, Package Skeletons, and Product Resources

OWNER: separate Codex / GPT-5.6 Sol Max integration session
WORKTREE: `C:\Users\93rob\Documents\GitHub\Lucidity-worktrees\AT-099-baseline`
BRANCH: `codex/AT-099-baseline`
BASE CRITERIA: the committed controller control plane whose product parent is `4b1c3c151eb530e569f867e1461693c56fe89695`. The controller records and asserts the exact launch `HEAD` in the external delegation ledger and worktree receipt; this packet never embeds its containing commit's self-referential SHA.

## Objective

Create the smallest accepted, buildable workspace baseline for the three root-sibling product packages; freeze protocol/domain constants; predeclare disjoint module roots; and give the product binary its own correct Windows resource/build scaffolding. Do not perform GUI library extraction or feature implementation.

## Exclusive write lease

- root `Cargo.toml` and `Cargo.lock`
- `pty/Cargo.toml` only to add the already-pinned WinAPI `jobapi2` feature required by AT-107
- new `agent-protocol/**`, `agent-backends/**`, `agent-terminal/**` skeletons
- only the product resource/build files explicitly required by ADRs and `INTERFACES.md`

`HACKATHON/**` remains protected user input. Upstream product sources, `.orchestrator/**`, `window/**`, `mux/**`, and `wezterm-gui/**` are forbidden. The sole upstream-manifest exception is the exact `pty/Cargo.toml` WinAPI feature declaration above; no version or dependency may change there. Cargo auto-discovers `wezterm-gui/src/lib.rs`; this task must not add a `[lib]` stanza or touch `wezterm-gui/Cargo.toml`.

## Requirements

1. Add exactly `agent-protocol`, `agent-backends`, and `agent-terminal` as root-sibling workspace members.
2. `agent-protocol` compiles with dependency-light IDs, state/view/wire types, `PendingIdentity`, exit observation, framing constants, `MAX_AGENT_EVENT_JSON_BYTES = 32_768`, `MAX_PENDING_AGENT_EVENTS = 256`, and reserved user-var name `lucidity.agent-event.v1`. Per ADR-004, GUI close policy is not a protocol type.
3. Predeclare only the stable top-level package roots needed for disjoint ownership. `agent-terminal/src/lib.rs` declares `catalog`, `chrome`, `host`, `ipc`, `runtime`, and `windows`, and AT-099 creates a compile-only `mod.rs` in each directory. These files contain no speculative implementation or deep taxonomy; the later worker leasing a directory owns and may replace that directory's compile-only `mod.rs`. Predeclare the existing-workspace dependencies justified by AT-101/102/103/108 so those workers do not edit manifests.
4. Create stub tests/fixtures for serialization compatibility and state orthogonality.
5. Create an Agent binary stub with its own `windows_subsystem`, build script, icon/resource `0x101`, PerMonitorV2/UTF-8 manifest, version data, runtime-file provisioning/freshness helpers, distinct AUMID/class/title constants, and explicit update-check-disabled configuration. The thin binary wrapper calls exactly `agent_terminal::chrome::run() -> anyhow::Result<()>`; AT-099 supplies a compile-only stub and AT-101 later implements that function within its `chrome/**` lease without editing the binary wrapper. It need not open the GUI yet.
6. Keep every package MIT-compatible and record new third-party dependency licenses.
7. Add `jobapi2` to `portable-pty`'s existing target-Windows `winapi` feature list. This is a compile-surface declaration for ADR-003, not Job Object implementation; do not add a crate or change the pinned `winapi` version.
8. Predeclare Lucidity's internal `CloseIntent::{StockClose, HideToTray, QuitOwned}` under `agent-terminal` lifecycle/host ownership. Do not add `agent-terminal -> wezterm-gui` yet: the live GUI package is binary-only and Cargo would ignore that dependency with a warning.
9. Implement the frozen achromatic Weld Frame mark from `.orchestrator/DESIGN/NATIVE_UI_CONTRACT.md` as the product icon source and ICO resource. Do not reuse the WezTerm icon or invent a worker-specific mark.

## Acceptance

```powershell
cargo fmt --all -- --check
cargo check -p agent-protocol -p agent-backends -p agent-terminal
cargo test -p agent-protocol
cargo build --release -p agent-terminal
cargo check -p portable-pty
```

Inspect the Agent stub PE resources/version/subsystem directly. Confirm stock `wezterm-gui` and `wezterm` still build at this commit and rerun the non-interactive stock baseline receipts. Return exact lockfile/dependency delta and raw outcomes.

## Stop and escalate

Any need to edit upstream GUI/window/mux code, add more packages, change a frozen contract, copy product implementation into skeletons, or accept an incompatible license.
